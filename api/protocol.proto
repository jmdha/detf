syntax = "proto3";

package protocol;
option go_package = "/protocol";

service DETF {
  rpc Stream(stream Result) returns (stream Match) {}
}

message Match {
  uint64 ID       = 1;
  string Position = 2;
}

message Result {
  uint64 ID   = 1;
  bool   Win  = 2;
  bool   Draw = 3;
}
